# frozen_string_literal: true

RSpec.describe Markdown::Jotter do
  it "has a version number" do
    expect(Markdown::Jotter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
